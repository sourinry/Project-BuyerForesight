# Project-BuyerForesight
Backend Engineering Project For BuyerForesight (Build a REST API Managing User)

# User Management API

## Setup

```bash
npm install
npm start (for start the server without nodemon)
npm run dev (for start the server with nodemon)